println = {std.print(@[0])std.print("\n")}

n_iter = 1

x;y;val;res;count=0;hits=1
std.repeat({

    x = std.random() * 2 - 1
    y = std.random() * 2 - 1

    res = std.if(x*x + y*y <= 1, 1,0)
    hits = res + hits
    println(hits)

    count = count + 1
	count < n_iter
})
